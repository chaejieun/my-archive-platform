server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myarchive?serverTimezone=Asia/Seoul
    username: root
    password: 1q2w3e4r!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher  # 스프링 2.6 이상에서 URL 에러 방지용

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
